# Environment Variables
# Copy this to .env and update with your actual values

# Database Configuration
POSTGRES_USER=matland_user
POSTGRES_PASSWORD=your_secure_password
POSTGRES_DB=matland_store
DATABASE_URL=postgresql://matland_user:your_secure_password@db:5432/matland_store

# Node Environment (use 'development' for local dev, 'production' for server)
NODE_ENV=production